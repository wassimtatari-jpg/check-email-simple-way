email=input("Enter your email :")
clean_email=email.strip()
its_valid=(
    (email==clean_email)and
    (len(email)>=5)and
    (clean_email.find("@")!=-1)
)
print(its_valid and "valid email"or "invalid email")

